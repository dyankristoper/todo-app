const initialState = {

};


const reducer = (state = initialState, action) => {

    switch(action.type) {
        case 'ADD':
            break;
        case 'REMOVE':
            break;
        case 'PAID':
            break;
        case 'UNDO':
            break;
        default:
            return state;
    }
}
export default reducer;